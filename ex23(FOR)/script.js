 let cores = [
    {nome: 'Preto', qt:5},
    {nome: 'Azul', qt:10},
    {nome: 'Vermelho', qt:35}
 ]

for (let cor of cores) {
    console.log(`Quantidade: ${cor.nome} ${cor.qt}`)
} 
 

 /*
 for (let i in cores) {
    console.log(cores[i]);
 }
 */

 /* for (let n = 0; n < cores.length; n++) {
 console.log(cores[n]);
 } */

 
 /* for(let x = 0; x <= 10; x++) {
  for(let y = 0; y <= 10; y++) {    
        console.log(`${x} * ${y} = ${x * y}`);    
 }
 }

 tabuada */







