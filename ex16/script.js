/*function somar(x, y){
    return x + y;
}

ARROw FUNCTION

() => x + y;

*/

const somar = (x, y) => x + y;

console.log(somar (10, 5));

const sobrenome = sob => 'Bonieky ' + sob;

console.log(sobrenome('Lacerda'));